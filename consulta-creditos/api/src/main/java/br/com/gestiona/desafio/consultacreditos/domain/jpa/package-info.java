/**
 * Contains JPA entity classes that map to database tables.
 * These classes represent the persistent data model of the application and are used
 * to interact with the underlying database through JPA (Java Persistence API).
 * Each entity typically corresponds to a database table and includes JPA annotations
 * to define the mapping between Java objects and database tables.
 */
package br.com.gestiona.desafio.consultacreditos.domain.jpa;